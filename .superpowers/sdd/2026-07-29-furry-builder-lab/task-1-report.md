# Task 1 report — source atlases

## Status

DONE

## Files changed

- `tools/furry-builder-lab/assets/heads-atlas.png`
- `tools/furry-builder-lab/assets/eyes-atlas.png`
- `tools/furry-builder-lab/assets/mouths-atlas.png`
- `tools/furry-builder-lab/assets/atlas-manifest.json`
- `tools/furry-builder-lab/test/assets.test.cjs`
- `.superpowers/sdd/2026-07-29-furry-builder-lab/task-1-report.md`

## Test-first evidence

The contract test was created first and run before the manifest existed.

Command:

```text
node --test tools/furry-builder-lab/test/assets.test.cjs
```

RED output (expected):

```text
fail 1
Error: Cannot find module '../assets/atlas-manifest.json'
```

Final command:

```text
node --test tools/furry-builder-lab/test/assets.test.cjs
```

Final output:

```text
pass 1
fail 0
✔ declares three twelve-cell atlases with the required chroma key
```

PNG readability/grid check:

```text
eyes-atlas.png   1086x1448  width % 3 = 0  height % 4 = 0  Format24bppRgb
heads-atlas.png  1086x1448  width % 3 = 0  height % 4 = 0  Format24bppRgb
mouths-atlas.png 1086x1448  width % 3 = 0  height % 4 = 0  Format24bppRgb
```

## Generated image source paths

- Heads: `C:\Users\logga\.codex\generated_images\019faf6f-bbc0-7421-bb2c-6d11aaab91d9\exec-c5223139-7389-4d2c-b2d9-e77ce3c1d60c.png`
- Eyes (final, replacement for the rejected first result): `C:\Users\logga\.codex\generated_images\019faf6f-bbc0-7421-bb2c-6d11aaab91d9\exec-d994d9d0-a1f6-4d4b-b942-6fd2e99ee79e.png`
- Mouths: `C:\Users\logga\.codex\generated_images\019faf6f-bbc0-7421-bb2c-6d11aaab91d9\exec-4bafacfc-3507-4694-b48a-b3d567254d76.png`

## Visual inspection

All final assets were opened and inspected at high detail. Every image has a flat bright-green chroma-key background, no text, and a regular 3-column by 4-row addressable layout.

- Heads: twelve distinct fur/head silhouettes; no ears, eyes, nose, or mouth are present.
- Eyes: twelve centered front-facing eye-pair overlays, one pair in each logical 3-by-4 cell; the first generated eye sheet was rejected because it was not sufficiently grouped into pairs, then regenerated.
- Mouths: twelve distinct centered front-facing mouth overlays, including neutral, smile, open-mouth, teeth, tongue, and pout expressions.

## Commit SHA

`8325b497ad67b737a349da12c4aa8e8f2d5e1fed` — source-atlas and contract-test commit.

## Concerns

The eye pairs contain prominent dark upper eyelids/lashes; this is intentional eye styling rather than separate facial components. The flat chroma key is stored as RGB source art for the later canvas chroma-key removal step, not as transparency.

---

## Fix round 1 — alpha normalization and head-shape revision

### Status

DONE

### Findings reproduced

The original runtime PNGs were RGB (`Format24bppRgb`) and retained an ImageGen near-green background gradient. The original test only checked manifest literals and could not detect either condition.

### Changes

- Preserved the raw ImageGen sheets under `tools/furry-builder-lab/assets/source/`.
- Replaced the runtime atlas files with RGBA PNGs generated by the required ImageGen helper invocation for every sheet:

  ```text
  python C:\Users\logga\.codex\skills\.system\imagegen\scripts\remove_chroma_key.py \
    --auto-key border --soft-matte --transparent-threshold 12 \
    --opaque-threshold 220 --despill --force
  ```

- Updated the manifest to `renderMode: "alpha"`.
- Replaced the heads raw sheet with a newly generated 3-by-4 atlas of twelve visibly distinct, earless and featureless furry head-base silhouettes.
- Expanded `assets.test.cjs` to decode each declared PNG and validate its signature, RGBA color type, 3-by-4 divisibility, transparent four corners, and nonempty alpha coverage in every cell.

### Fix test evidence

RED command:

```text
node --test tools/furry-builder-lab/test/assets.test.cjs
```

RED output:

```text
pass 0
fail 1
actual: undefined
expected: alpha
```

GREEN command:

```text
node --test tools/furry-builder-lab/test/assets.test.cjs
```

GREEN output:

```text
✔ declares three readable twelve-cell alpha atlases
pass 1
fail 0
```

### Visual inspection

The RGBA runtime files were rendered over white for inspection. All three show cleanly transparent backgrounds, preserve their visible art, and have components in all twelve addressable cells. The revised heads have distinct rounded, long, broad, heart-like, pointed-chin, cloud-like, tall, low-wide, swept-fringe, layered-mane, compact-cap, and dense-curled outlines. No head has ears, eyes, nose, or mouth. The existing eye and mouth imagery was unchanged except for background extraction.

### Generated source paths

- Revised heads raw: `C:\Users\logga\.codex\generated_images\019faf6f-bbc0-7421-bb2c-6d11aaab91d9\exec-48b515ff-4480-4ef9-9448-296ed396d8bc.png`
- Eyes raw retained from: `C:\Users\logga\.codex\generated_images\019faf6f-bbc0-7421-bb2c-6d11aaab91d9\exec-d994d9d0-a1f6-4d4b-b942-6fd2e99ee79e.png`
- Mouths raw retained from: `C:\Users\logga\.codex\generated_images\019faf6f-bbc0-7421-bb2c-6d11aaab91d9\exec-4bafacfc-3507-4694-b48a-b3d567254d76.png`

### Fix commit SHA

`4f2658543f501449ecbdf06183bad99e434a3224`

### Concerns

The helper samples the true near-green border color for each source (`#02f907`, `#0af70e`, and `#06f90a` respectively), rather than assuming exact `#00ff00`; that is the required protection against ImageGen's slight green gradient. Soft alpha edges are intentional for antialiased sprite compositing.
