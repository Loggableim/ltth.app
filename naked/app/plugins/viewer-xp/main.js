// Compatibility shim for legacy imports. The maintained plugin lives in
// viewer-leaderboard, but older integrations still require plugins/viewer-xp.
module.exports = require('../viewer-leaderboard/viewer-xp-impl');
